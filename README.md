# Insights Frontend Web

### Variables de entorno

En `insightsweb/` se debe crear el archivo `env.local` con el siguiente contenido (sin los comentarios en "//") para poder correr el programa de forma local:

```
//URL local de ejecución del backend
NEXT_PUBLIC_API_URL= X
```